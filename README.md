# Lab 4: Express Solar System

Express + EJS app for the CST336 Solar System lab.

## Run locally

```bash
npm install
npm start
```

Then open `http://localhost:3000`.

## Deploy

This project includes `render.yaml` and starts with:

```bash
node index.mjs
```
